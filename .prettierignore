dist
storybook-static
coverage
pnpm-lock.yaml

# Story fixtures — large generated data, not hand-edited.
src/data
src/styleguide/orders.json

# Generated files. Formatting these would make every regeneration produce a
# spurious diff, because the generator writes its own (valid but different)
# style. Their scripts own the formatting.
#   pnpm icons:bundle
#   node scripts/generate-timezone-countries.mjs
src/components/Icon/iconBundle.ts
.storybook/storyIconBundle.ts
src/components/TimezonePicker/zoneCountries.ts

# Prettier parses this as markdown, not MDX: it escapes the `{/* */}` comments
# to `{/\* \*/}` and strips the indentation off the JSX inside the
# `export const CodeTabs` block, which then fails to parse with acorn and
# breaks the Storybook build. The other .mdx files here are plain prose and
# format fine — this is the only one carrying JSX/ESM.
src/stories/QuickStart.mdx
